export class GitHub {
 constructor(readonly repo:string,private readonly token:string){if(!/^[\w.-]+\/[\w.-]+$/.test(repo))throw new Error('Invalid repository');}
 async request(path:string,method='GET',body?:unknown):Promise<any>{const response=await fetch(`https://api.github.com/repos/${this.repo}${path}`,{method,headers:{Authorization:`Bearer ${this.token}`,Accept:'application/vnd.github+json','X-GitHub-Api-Version':'2022-11-28','Content-Type':'application/json'},...(body?{body:JSON.stringify(body)}:{}),signal:AbortSignal.timeout(30000)});if(!response.ok)throw new Error(`GitHub ${method} returned HTTP ${response.status}`);return response.status===204?null:response.json();}
 async list(path:string,maxPages=10){const items:any[]=[];for(let page=1;page<=maxPages;page++){const result=await this.request(`${path}${path.includes('?')?'&':'?'}per_page=100&page=${page}`);items.push(...result);if(result.length<100)break;if(page===maxPages)throw new Error('History pagination cap reached; archive or increase deliberately');}return items;}
}
