import Link from 'next/link';
import {notFound} from 'next/navigation';
import {findIdea,repository} from '@/lib/data';
import {FeedbackForm} from '@/components/feedback';
export const dynamic='force-dynamic';
export default async function Detail({params}:{params:Promise<{id:string}>}){const {id}=await params;const data=await findIdea(id);if(!data)notFound();const {idea,batch,challenge,page}=data;return <main className="detail"><header className="topbar"><Link href="/">← The collection</Link><span>{batch.mode==='example'?'ILLUSTRATIVE EXAMPLE':'CANDIDATE'} / {new Date(batch.createdAt).toLocaleDateString('en-GB')}</span></header><div className="detail-hero"><p className="section-label">{idea.audience}</p><h1>{idea.name}</h1><p className="subhead">{idea.tagline}</p>{page&&<Link className="primary" href={`/preview/${id}`}>Explore the page concept ↗</Link>}</div>{batch.mode==='example'&&<p className="notice">Example brief for testing the workflow. These claims have not been researched, and the score is illustrative.</p>}<div className="detail-grid"><div>{[['The problem',idea.problem],['The proposition',idea.solution],['Why now',idea.whyNow],['Route to a first audience',idea.distribution],['Pricing hypothesis',idea.priceHypothesis],['Cheapest falsification test',idea.falsificationTest]].map(([title,body])=><section key={title}><h2>{title}</h2><p>{body}</p></section>)}</div><div className="challenge"><p className="section-label">THE SCEPTIC’S VIEW</p><div className="big-score">{challenge.score}<span>/100</span></div><small>Triage score · not success probability</small><h3>{challenge.verdict==='build'?'Worth a page test':challenge.verdict==='reject'?'Do not build yet':'More evidence needed'}</h3><p>{challenge.rationale}</p><hr/><h3>The strongest objection</h3><p>{challenge.strongestObjection}</p><h3>What we don’t know</h3><ul>{idea.uncertainties.map(u=><li key={u}>{u}</li>)}</ul></div></div><section className="sources"><p className="section-label">EVIDENCE & PROVENANCE</p>{idea.sources.map(s=><a key={s.url} href={s.url} target="_blank" rel="noreferrer"><span>{s.kind}</span><h3>{s.title} ↗</h3><p>{s.claim}</p></a>)}</section>{batch.critique&&<section className="critique"><h2>Automated design review</h2><p>{batch.critique.pass?'Initial visual review passed.':'Initial visual review requested changes; inspect the final page.'}</p><ul>{batch.critique.changes.map(c=><li key={c}>{c}</li>)}</ul><p><strong>Proposed lesson:</strong> {batch.critique.proposedRule}</p></section>}<FeedbackForm id={id} repo={repository()} example={batch.mode==='example'}/><details className="audit"><summary>Run history</summary>{batch.events.map((e,i)=><p key={i}><code>{e.type}</code> {e.detail}</p>)}</details></main>}
