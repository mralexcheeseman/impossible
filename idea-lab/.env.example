# Vercel: set a random secret of at least 32 characters. All pages fail closed without it.
LAB_ACCESS_SECRET=
# Only fixture examples are visible in hosted demo mode; generation is never enabled by this flag.
IDEA_LAB_DEMO=false
# GitHub repository used by the feedback draft link.
NEXT_PUBLIC_LAB_REPOSITORY=mralexcheeseman/impossible
# GitHub Actions only. Do not add these to the web application.
OPENAI_API_KEY=
LAB_MODEL=gpt-4.1
LAB_ENABLED=false
