# Idea Lab companion · 23 September 2026

Alex requested a continuous idea and landing-page pipeline and authorised implementation. This bounded companion is isolated under `idea-lab/`; the original IMPOSSIBLE seven-day experiment architecture and M0/M1 requirements remain intact and unimplemented.

This authorised slice uses GitHub issues, reviewed JSON batches and Vercel preview branches instead of claiming to provide the core Postgres event ledger, experiment deadlines, Supabase auth or business approvals. It can draft concepts and append operational run records. It cannot contact users, publish social posts, accept payments, alter its own rules or promote production automatically.

The existing public repository is a code staging location. Live research and owner feedback require a private repository. No sensitive personal context is included in the fixtures or prompts.

See `idea-lab/README.md` for setup, exact limits and known gaps. Acceptance: validated provider outputs, public-repository and pause gates, bounded run/call counts, labelled fixtures, protected production access, working concept pages, structured feedback drafts and documented activation requirements.
